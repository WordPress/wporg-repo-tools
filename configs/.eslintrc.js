/**
 * Legacy ESLint config, for ESLint 8 / `@wordpress/scripts` below 32.
 *
 * Generated by `bin/update-configs`. The rules arrive with `composer update` —
 * edit them in wporg-repo-tools, not here.
 *
 * Project-specific rules go inside the `rules` object below. A second `rules`
 * key after the spread would replace every shared rule at once, silently.
 */

/**
 * Internal dependencies
 */
const createConfig = require( './vendor/wporg/wporg-repo-tools/configs/eslintrc' );

const config = createConfig( {
	textDomain: '${TEXTDOMAIN}',
	prettierConfig: require( './.prettierrc' ),
} );

module.exports = {
	...config,
	rules: {
		...config.rules,
	},
};
