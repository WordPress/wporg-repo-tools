<?xml version="1.0" ?>
<ruleset name="wporg">
    <description>Coding standards for WordPress.org Meta projects.</description>

    <!--
        Registered as a PHPCS standard by the installer plugin, so projects
        reference it as `<rule ref="wporg" />` rather than copying it. Which
        files to scan and the text domain belong in the project's own ruleset.
    -->

    <rule ref="WordPress">
        <exclude name="Universal.Arrays.DisallowShortArraySyntax.Found" />
        <exclude name="Universal.Operators.DisallowShortTernary.Found" />
        <exclude name="WordPress.Files.FileName.InvalidClassFileName" />

        <exclude name="Generic.Commenting.DocComment.MissingShort" />
        <exclude name="Squiz.Commenting.FileComment.Missing" />
        <exclude name="Squiz.Commenting.FileComment.MissingPackageTag" />
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamTag" />
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamComment" />
        <exclude name="Squiz.Commenting.InlineComment.InvalidEndChar" />
    </rule>

    <!--
        Set here, not per project: PHPCompatibility checks nothing at all when
        `testVersion` is unset, so a project that forgot it would lose the
        sniffs silently. PHPCS applies a ruleset's config before descending into
        a `<rule ref>`, so this wins over a project's own value; only the
        `runtime-set` command line option overrides it.
    -->
    <config name="testVersion" value="8.4-" />
    <rule ref="PHPCompatibilityWP" />

</ruleset>
