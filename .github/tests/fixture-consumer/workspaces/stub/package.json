{
	"name": "wporg-test-consumer-fixture-stub-workspace",
	"version": "1.0.0",
	"private": true,
	"description": "Stub workspace exercised by the build and lint steps in the composite actions.",
	"scripts": {
		"build": "echo 'build ran successfully'",
		"lint:css": "wp-scripts lint-style \"src/**/*.pcss\"",
		"lint:js": "wp-scripts lint-js src"
	}
}
