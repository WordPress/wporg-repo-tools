{
	"name": "wporg-test-consumer-fixture",
	"version": "1.0.0",
	"private": true,
	"description": "Minimal consumer fixture used to validate the composite actions in this repo.",
	"workspaces": [
		"workspaces/*"
	],
	"scripts": {
		"setup:tools": "echo 'setup:tools ran successfully'",
		"lint:php": "echo 'lint:php ran successfully'",
		"test:php": "wp-env run cli --env-cwd=/var/www/html/wp-content/plugins/$(basename \"$PWD\") phpunit -c tests/phpunit.xml.dist"
	},
	"devDependencies": {
		"@wordpress/env": "^11.0.0"
	}
}
